{
  "name": "claw-webchat",
  "description": "Standalone WebChat for OpenClaw with long-lived history and media support",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check public/app.js && node --check scripts/selftest.mjs",
    "selftest": "node scripts/selftest.mjs",
    "bundle": "bash scripts/build-release-bundle.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
