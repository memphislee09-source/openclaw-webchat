# Handoff — 2026-03-24

## 当前结论
`openclaw-webchat` 现在应继续以 `main` 为唯一开发基线。2026-03-24 这一轮已把历史搜索第二阶段首批增强、agent 级 `/model` / thinking 快捷切换、发送/停止双态按钮、gateway `chat.abort` 停止链路、公开发布准备文档，以及桌面端图片查看器缩放读数修复一起收口到 `0.1.6`。当前 `codex/sse-event-refresh` 分支已继续推进到 `0.1.7`，补上事件驱动刷新、统一滚动控制器，以及缺消息回补收口。

当前已确认的主线状态：
- 当前版本已提升到 `0.1.7`
- 默认服务地址仍为 `http://127.0.0.1:3770`
- 当前开发基点仍为 `main`
- 用户级 LaunchAgent `ai.openclaw.webchat` 继续作为常驻运行方式
- 当前主线已经包含：
  - 历史搜索第二阶段首批增强：日期筛选、`20 / 50 / 100` 结果集、更强匹配策略与结果高亮
  - 当前 agent 级 `/model` / `/models` 模型切换弹窗，以及 session 级模型切换接口
  - 输入区右侧的静态 `T` thinking 控件：按钮不再显示动态 `T:*` 档位，避免切换 agent 时出现明显刷新延迟；当前 session 的模型与完整 thinking level 会显示在聊天头部 agent 名称右侧，按钮仍可弹出当前模型可用的 thinking level 列表
  - 输入框发送/停止双态按钮，以及 `POST /api/openclaw-webchat/sessions/:sessionKey/stop` 停止链路
  - gateway CLI 混合 stdout 兼容解析，避免插件诊断日志打断 `/model`、`/think` 等本地 slash 命令
  - README、公开发布 checklist、issue/PR 模板、bundle/network 两套 agent 安装说明与 release bundle 打包脚本
  - 图片查看器全屏模式下的动态缩放读数，滚轮缩放后右上角读数会同步显示真实比例
  - 用户可见品牌名已统一调整为 `Claw WebChat`；后端技术标识、API 路径、环境变量和仓库名仍保持 `openclaw-webchat`
  - 隐藏 Claw WebChat bootstrap 已更新为更短但更明确的媒体回传约定：本地文件与直链远程 `http/https` 媒体都应通过结构化 block 或 `MEDIA:` / `mediaUrl:` fallback 返回，避免误走 `message` / `webchat` 发送路径；本地生成的 `.mp3` / `.wav` TTS 音频也已明确纳入这一协议
  - 常规桌面图片 / 视频消息默认宽度上限已提升到 `70vw`，同时保留既有图文混排等宽气泡判定逻辑
  - 右侧消息区滚动乱跳已修复：历史补页保持原可见区，agent 处理中不再强制贴底，消息列表关闭平滑滚动以避免动画叠加
  - 设置面板 About 已显示当前版本号；thinking 按钮字重也已做收口，避免比主发送动作更抢眼
  - `/model` slash 输出现已按 provider 汇总完整模型目录，不再把 `gpt-5.4` 这类尾部模型藏在 top-10 截断后面；模型弹窗也改成单行条目并在切换成功后保留打开状态
  - `/model` 弹窗空闲态已去掉重复的第二份介绍文案，顶部说明保留，底部提示区只在需要显示状态时出现
  - thinking 菜单已和 `/model` 弹窗对齐：重复打开优先复用当前 session 的最近状态，切换成功后会在菜单内保留成功提示，等待用户自行点击外部返回
  - 对话栏滚动模型已进一步收口：中段阅读时后台轮询发现当前会话有更新，会先显示“当前会话有更新，按 End 或点这里查看”提示，不再直接重开并打断当前视口；当前会话重渲染已改为保住可见消息锚点，并新增 `Home` / `End` / `PageUp` / `PageDown` 键盘导航
  - 事件驱动刷新第一版已在 `codex/sse-event-refresh` 实现：服务端新增 `/api/openclaw-webchat/events` SSE 长连接，在 history / binding 发生真实变化时推送 `agent-update` / `conversation-update`，前端优先吃事件刷新；原先固定 10 秒轮询已降到 60 秒兜底
  - 对话栏滚动控制已继续收口：render、媒体加载、历史补页、当前会话 reload 与显式跳转都通过统一 viewport snapshot / restore 控制器处理，默认优先保当前位置，只有明确 follow-bottom 时才自动贴底
  - assistant 最终回复匹配已继续收口：同步等待与 late-reply reconciliation 都只接受当前 upstream user 之后、且不带 `toolCall` 的可持久化 assistant 消息，避免把进度文本误记成最终回复
  - 会话打开时会对当前 upstream session 做 history reconcile，把 upstream 已存在但本地 JSONL 缺掉的 user / assistant 行回补回来，修复 reopen 后仍永久缺消息的问题
- 当前验证状态：
  - `npm run check` 已通过
  - `sessionKey` 前缀不一致已修复：服务端会自动把历史 `claw-webchat:<agentId>` 绑定迁到 `openclaw-webchat:<agentId>`，同时继续兼容旧 key 请求
  - `npm run selftest` 现在应作为通过标准继续使用
  - 本地与远程媒体 fallback 指令已补回归断言，当前 `npm run selftest` 已覆盖这条协议
  - 桌面常规媒体 `70vw` 上限与右侧消息区滚动修复现已并入 `main`；相关自测 `npm run check`、`npm run selftest` 已通过，服务重启后 `http://127.0.0.1:3770/healthz` 正常
  - About 版本号显示、thinking 按钮/菜单链路，以及头部模型 / thinking 状态展示已完成验证；当前 `npm run selftest` 已继续通过
  - 对话栏滚动模型 follow-up 已通过 `npm run check` 与 `npm run selftest`
  - Wangyuyan mp3 回传 follow-up 已通过 `npm run check` 与 `npm run selftest`；隐藏 bootstrap 版本已提升，现有 session 会在下次 open/send 时重新注入更明确的本地音频回传规则
  - 事件驱动刷新 follow-up 已通过 `npm run check`、`npm run selftest`、LaunchAgent 重启后的 `http://127.0.0.1:3770/healthz` 检查，以及 `/api/openclaw-webchat/events` 的 SSE `connected` 帧冒烟验证
  - 统一滚动控制器 follow-up 已通过 `npm run check` 与 `npm run selftest`
  - 缺消息修复 follow-up 已通过 `npm run check`、`npm run selftest`、LaunchAgent 重启后的 `http://127.0.0.1:3770/healthz` 检查；`wangyuyan` 会话 reopen 后已成功补回此前缺失的 `2026-03-26T07:34:06.111Z` 新闻简报

## 已完成基线
- 独立项目、独立端口、独立命名空间 `openclaw-webchat`
- `agentId -> session` 长期绑定与自有 JSONL 历史存储
- `/new` 仅重置上游上下文，本地历史保留并插入 `已重置上下文`
- assistant 最终回复抓取，屏蔽中间工具过程
- 图片 / 音频 / 视频 / 文件渲染与媒体失效兜底
- 用户图片上传、用户音频上传、默认本地 Whisper 转写
- slash 命令目录与本地执行语义
- 设置面板、联系人显示设置、头像本地裁剪上传
- 外观设置：主题预设与中英文界面切换
- 访问与安全设置：访问模式、可选轻认证、设置内重启、手动启动说明
- 关于页、GitHub 链接、公开协作文档与最小 CI
- Markdown 渲染、图文混排顺序修复、共享 `message-blocks` 逻辑
- 移动端抽屉、消息贴底显示、左右栏独立滚动
- 历史搜索第一版与第二阶段首批增强
- 当前 agent 级模型切换弹窗
- 当前 agent 级静态 `T` thinking 按钮、头部模型 / thinking 状态展示与 thinking level 切换菜单
- 对话栏滚动状态机、延后后台刷新提示、可见消息锚点恢复，以及 `Home` / `End` / `PageUp` / `PageDown` 键盘导航
- 输入框发送/停止双态图标按钮与 session 级 stop / `chat.abort` 停止链路
- 设置面板 About 版本号显示
- 图片查看器全屏缩放、平移与动态读数显示
- 隐藏 bootstrap 的本地/远程媒体回传协议收口，agent 无需额外提醒即可在 Claw WebChat 中正确发送媒体
- 本地生成 `.mp3` / `.wav` 音频的回传协议已补强：agent 应直接用 `MEDIA:` / `mediaUrl:` 返回，而不是声称 WebChat 无法接收文件
- 事件驱动刷新第一版：SSE 负责主同步，60 秒轮询只保留为断线/漏事件时的安全兜底
- 统一滚动控制器第一版：保位置是默认策略，异步媒体/刷新回调必须经过 viewport revision 校验后才允许恢复旧快照
- 常规桌面媒体默认 `70vw` 宽度上限
- 右侧消息区滚动稳定性修复
- 公开发布准备第一批资料：README 首页、发布 checklist、截图、bundle 构建脚本、issue/PR 模板增强、两套 agent 安装指南

## 当前优先事项
1. 继续验证移动端历史加载稳定性；必要时进入第二阶段根因修复
2. 继续人工对比视觉媒体等宽气泡方案，特别是多图文交错与“小图 + 长正文”场景
3. 做更完整的多 agent / 迟到回复回归
4. 补音频转写成功链路人工验收
5. 继续增强历史搜索（更长结果分页 / 跳转定位细化 / 匹配策略继续打磨）

## 分支备注
- `main` 仍是唯一主线基线。
- `codex/desktop-media-70vw` 的有效改动已并入 `main`；后续继续开发应直接从 `main` 出发。

## 建议启动顺序
1. 先读 `status.md`
2. 再读本文件
3. 如需补背景，再读 `docs/ARCHITECTURE.md`、`docs/ROADMAP.md`、`docs/error.md`
4. 直接从“移动端历史加载 / 视觉媒体收口 / 多 agent 回归 / 音频转写验收 / 历史搜索增强后续”五项之一继续
